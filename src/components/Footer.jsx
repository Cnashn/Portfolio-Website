const Footer = () => (
  <footer className="relative z-0 py-8 flex justify-center">
    <p className="text-secondary text-[13px] tracking-wide">
      © {new Date().getFullYear()} Can Şahin
    </p>
  </footer>
);

export default Footer;
